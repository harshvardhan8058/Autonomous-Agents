[project]
name = "backend"
version = "1.0.0"
description = "Autonomous AI Agent - FastAPI backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "httpx>=0.27.0",
    "python-docx>=1.1.2",
]
